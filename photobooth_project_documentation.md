# نظام إدارة وتحليل مبيعات ومخزون الفوتوبوث (Photobooth Analytics & Management System)

وثيقة معمارية وتشغيلية شاملة لنظام متكامل مخصص لإدارة عمليات كشك التصوير (Photobooth)، تتبع المخزون اللحظي، وتسجيل تدفق الزبائن عبر الزمن لتمكين أصحاب المشروع من تحليل الأداء المالي والتشغيلي.

---

## 1. أهداف النظام (Project Objectives)

* **تسجيل المبيعات اللحظي:** واجهة استخدام سريعة ومبسطة جداً تمكن الموظف (الكاشير) من تسجيل كل عملية بيع (عدد الورق المطبوع والمبلغ المدفوع) بضغطة زر واحدة.
* **إدارة وتتبع مخزون الورق:** مراقبة رصيد الورق المتبقي بالمكان فورياً وخصمه تلقائياً مع كل عملية، مع إتاحة تزويد المخزون من الواجهة الرسومية (GUI).
* **تحليل تدفق الزبائن والأيام (Footfall & Timeline Analytics):**
  * رصد توقيت وصول **أول زبون** في كل يوم بدقة ودراسة مدى ارتباطه بإجمالي إيرادات اليوم.
  * رسم بياني زمني لمتابعة حركة صعود وهبوط المبيعات والزبائن عبر الأيام (Timeline Chart).
  * تحليل ساعات الذروة اليومية (Peak Hours Analysis).
* **فصل الصلاحيات:** فصل واجهة الإدخال اليومية الخاصة بالموظف عن لوحة التحكم والتحليلات المخصصة للمالك.

---

## 2. الهيكلية المعمارية للنظام (System Architecture)

يعتمد النظام على بنية خفيفة وقابلة للنشر المحلي أو السحابي:

```
┌──────────────────────────────────────────────────────────┐
│                   واجهات المستخدم (UI)                   │
├────────────────────────────┬─────────────────────────────┤
│   👤 شاشة الموظف (POS)     │   📊 لوحة الإدارة والتحليل   │
│  - إدخال العمليات          │  - مؤشرات الأداء (KPIs)     │
│  - شحن وتزويد المخزون      │  - منحنى المبيعات والزبائن  │
│  - سجل عمليات اليوم        │  - تحليل أول زبون والذروة   │
└─────────────┬──────────────┴──────────────┬──────────────┘
              │                             │
              └──────────────┬──────────────┘
                             ▼
┌──────────────────────────────────────────────────────────┐
│                 محرك التطبيق (Streamlit App)             │
│               - التحقق من كفاية المخزون                  │
│               - معالجة وإعادة هيكلة البيانات             │
│               - توليد الرسوم البيانية (Plotly)           │
└────────────────────────────┬─────────────────────────────┘
                             ▼
┌──────────────────────────────────────────────────────────┐
│                 قاعدة البيانات (SQLite / SQL)            │
│               - days (جدول الأيام)                       │
│               - transactions (جدول العمليات)             │
│               - inventory (جدول المخزون)                 │
└──────────────────────────────────────────────────────────┘
```

---

## 3. تصميم قاعدة البيانات (Database Schema)

### 3.1 جدول الأيام (`days`)
يحتوي على سجل لكل يوم عمل لتجميع العمليات تحته:

| الحقل | النوع | القيود | الوصف |
| :--- | :--- | :--- | :--- |
| `id` | `INTEGER` | `PRIMARY KEY AUTOINCREMENT` | المعرف التسلسلي لليوم |
| `date` | `TEXT / DATE` | `UNIQUE NOT NULL` | تاريخ اليوم بصيغة `YYYY-MM-DD` |

### 3.2 جدول العمليات (`transactions`)
يُسجل كل زبون أو معاملة طباعة:

| الحقل | النوع | القيود | الوصف |
| :--- | :--- | :--- | :--- |
| `id` | `INTEGER` | `PRIMARY KEY AUTOINCREMENT` | المعرف التسلسلي للعملية |
| `day_id` | `INTEGER` | `NOT NULL, FOREIGN KEY (days.id)` | معرف اليوم التابع له |
| `timestamp` | `TEXT / DATETIME` | `NOT NULL` | الوقت الدقيق للعملية `YYYY-MM-DD HH:MM:SS` |
| `prints_count` | `INTEGER` | `NOT NULL` | عدد الصور/الورق المطبوع |
| `amount_paid` | `REAL / DECIMAL` | `NOT NULL` | المبلغ المدفوع من الزبون |

### 3.3 جدول المخزون (`inventory`)
سجل حركات المخزون (Ledger) لحساب الرصيد الفعلي في أي لحظة:

| الحقل | النوع | القيود | الوصف |
| :--- | :--- | :--- | :--- |
| `id` | `INTEGER` | `PRIMARY KEY AUTOINCREMENT` | المعرف التسلسلي للحركة |
| `timestamp` | `TEXT / DATETIME` | `NOT NULL` | توقيت الحركة |
| `action_type` | `TEXT` | `NOT NULL` | نوع الحركة (`restock` إضافة أو `consumption` استهلاك) |
| `quantity` | `INTEGER` | `NOT NULL` | الكمية (موجبة عند التوريد، سالبة عند الطباعة) |
| `notes` | `TEXT` | `NULLABLE` | ملاحظات (مثل: استلام رول، رقم الشحنة) |

---

## 4. منطق العمليات والبيانات (Business Logic)

### أ. تسجيل عملية بيع جديدة:
1. استرجاع رصيد الورق الحالي: `SELECT COALESCE(SUM(quantity), 0) FROM inventory`.
2. التحقق من كفاية الرصيد (`Current Stock >= prints_count`).
3. التحقق من وجود سجل لتاريخ اليوم في جدول `days`؛ إن لم يوجد يتم إنشاؤه تلقائياً.
4. إدراج صف جديد في جدول `transactions`.
5. خصم عدد الورق تلقائياً بإدراج صف بحركة سالبة `-prints_count` في جدول `inventory`.

### ب. شحن المخزون (Restock):
1. يستقبل النظام الكمية المضافة والملاحظات من الواجهة.
2. يتم إدراج صف بحركة موجبة `+quantity` ونوع `restock` في جدول `inventory`.
3. يتحدث الرصيد الظاهر فورياً في الشريط الجانبي وجميع الشاشات.

### ج. تجميع مؤشرات الأداء (Analytics Aggregations):
يتم احتساب ملخص الأيام باستخدام الاستعلام التالي:
```sql
SELECT 
    d.date,
    MIN(t.timestamp) AS first_customer_time,
    MAX(t.timestamp) AS last_customer_time,
    COUNT(t.id) AS total_customers,
    COALESCE(SUM(t.prints_count), 0) AS total_prints,
    COALESCE(SUM(t.amount_paid), 0) AS total_revenue
FROM days d
LEFT JOIN transactions t ON d.id = t.day_id
GROUP BY d.date
ORDER BY d.date ASC;
```

---

## 5. شاشات وواجهات الاستخدام (UI Modules)

### 1. شاشة الموظف (الكاشير / الإدخال):
* **تسجيل عملية:** إدخال عدد الورق والمبلغ، وزر تأكيد مباشر.
* **إشعار نقص المخزون:** رسالة خطأ تمنع إتمام العملية في حال نفاد الورق.
* **تزويد المخزون:** نافذة لإضافة رول أو كرتونة ورق جديدة مع خانة للملاحظات.
* **سجل اليوم:** جدول يعرض آخر العمليات المسجلة خلال اليوم الحالي لتأكيد المعاملات.

### 2. لوحة التحكم والتحليلات (Admin Dashboard):
* **المؤشرات العلوية (KPIs):**
  * إجمالي الإيرادات الكلية.
  * إجمالي عدد الزبائن.
  * إجمالي عدد الورق المطبوع.
  * رصيد المخزون المتبقي بالمكان.
* **منحنى البورصة للأيام (Timeline):** رسم بياني مزدوج المحاور (Dual-Axis) يوضح تقلب الإيرادات وعدد الزبائن يومياً.
* **تتبع أول زبون:** جدول تفصيلي يوضح توقيت أول زيارة يومياً للمقارنة مع حجم المبيعات الإجمالي.
* **تحليل ساعات الذروة (Peak Hours):** رسم بياني شريطي يوزع كثافة الزيارات على ساعات اليوم (0-23h).

---

## 6. دليل التثبيت والتشغيل (Setup & Deployment)

### 6.1 المتطلبات البرمجية:
* بيئة Python 3.9 أو أحدث.
* المكتبات:
```bash
pip install streamlit pandas plotly
```

### 6.2 تشغيل التطبيق محلياً:
```bash
streamlit run app.py
```

### 6.3 خيارات النشر والاستضافة السحابية:
* **Streamlit Community Cloud:** استضافة مجانية عبر ربط المستودع من GitHub مباشرة.
* **Render / Railway:** نشر التطبيق كخدمة ويب دائمة التشغيل.
* **VPS (Hetzner / DigitalOcean):** تشغيل التطبيق كخدمة نظام داخلية مع استخدام النطاق الخاص بك وحماية لوحة الإدارة بكلمة مرور.